[extension]
Name=PSC-Bios
Description=WiFi, time zone and gamepads of the console
Author=AutoBleem team
Version=2.0.0
# resolved by AppManifest's rule; .so is added. The console only: a Pi or a PC has its own settings.
Plugin=bin/{key}/pscbios
Icon=icon.png
# works offline (it is where the WiFi gets set up); nothing to do while the carousel shows
Network=none
